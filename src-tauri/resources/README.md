# Packaged resources

`pnpm package` places the ignored PyInstaller `jarvis-core` onedir bundle here before Tauri
builds the Windows installer. Source checkouts intentionally contain no executable sidecar.
