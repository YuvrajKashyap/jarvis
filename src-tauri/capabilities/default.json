{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum capabilities for the local JARVIS overlay",
  "windows": ["overlay"],
  "permissions": [
    "core:default",
    "notification:default"
  ]
}
